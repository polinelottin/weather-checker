import defaultStyles from '../../Styles'

const styles = {
    container: {
        backgroundColor: defaultStyles.colors.lightBlue, 
        padding: 16, 
        margin: 0
    }
};

export default styles;